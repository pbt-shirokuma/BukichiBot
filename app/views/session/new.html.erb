<%= flash[:error] %>

<%= form_with(url: create_session_path, local: true) do |form| %>
  <table>
    <tr><th>ユーザー名</th><td><%= form.text_field :name %></td></tr>
  </table>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>